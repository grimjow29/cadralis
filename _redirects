# Netlify redirects file
# This file handles redirects and rewrites for the Cadralis site

# SPA fallback - serve index.html for all routes
/*    /index.html   200

# Optional: Redirect old URLs if you have any
# /old-page    /    301